(function () {
    'use strict';

    var components = {
        init: init
    };

    function init() {
        return name = "name";
    }

    window.components = window.components || {};
    $.extend(window.components, components);
})();
console.log("here");

////# sourceURL=http://localhost:56377/bundles/myscript.js